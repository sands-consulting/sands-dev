dev.my-sands.com {
    root /opt/sands-dev
    gzip
    fastcgi / /var/run/php/php7.0-fpm.sock php {
        index server.php
    }
    rewrite {
        r .*
        ext /
        to /server.php?{query}
    }
}
